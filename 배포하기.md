# 사이트 배포 — 사장님이 하실 일

전부 무료입니다. API 사용료만 월 3만원 정도 나갑니다.
순서대로 하시면 40분쯤 걸립니다. **3번까지만 해도 사이트가 뜹니다.**

---

## 1. Anthropic API 키 발급 (5분)

1. https://console.anthropic.com 접속 → 로그인
2. 왼쪽 메뉴 **API Keys** → **Create Key** → 이름 `viral-insight`
3. 나온 키(`sk-ant-...`)를 **메모장에 복사해 두세요.** 창을 닫으면 다시 못 봅니다.
4. **Billing** 메뉴에서 결제수단 등록 + 크레딧 충전 (5만원이면 두 달 씁니다)

> **월 사용량 상한을 걸어두세요.** Billing → Usage limits → 월 5만원.
> 사이트가 공개돼 있으니 만약의 오남용에 대비하는 안전장치입니다.

---

## 2. GitHub 저장소 만들기 (10분)

1. https://github.com 가입 (있으면 생략)
2. 오른쪽 위 **+** → **New repository**
   - 이름: `viral-insight`
   - **Private 선택** ← 권장
3. **uploading an existing file** 링크 클릭
4. `_tool` 폴더 안에서 **아래 파일만** 끌어다 놓기

```
app.py   config.py   extract.py   matcher.py   sheet.py
run_folder.py   selftest.py   requirements.txt   README.md
```

> ❌ 올리면 안 되는 것: `service_account.json`, `_inbox`, `*.csv`, `__pycache__`
> (`.gitignore` 가 걸러주지만 웹 업로드는 수동이니 직접 확인하세요)

5. 아래 **Commit changes** 클릭

---

## 3. Streamlit Cloud 배포 (10분)

1. https://share.streamlit.io → **Sign in with GitHub**
2. **Create app** → **Deploy a public app from GitHub**
3. 입력
   - Repository: `본인아이디/viral-insight`
   - Branch: `main`
   - Main file path: `app.py`
4. **Advanced settings** → **Secrets** 에 아래를 붙여넣기

```toml
ANTHROPIC_API_KEY = "sk-ant-여기에-1번에서-복사한-키"
ADMIN_PASSWORD = "팀에서-쓸-비밀번호"
```

5. **Deploy** 클릭 → 2~3분 기다리면 주소가 나옵니다
   (`https://무언가.streamlit.app`)

**여기까지 하면 사이트가 뜹니다.** 비밀번호 넣고 들어가면 판독까지 됩니다.
다만 시트 연결이 안 돼 있으니 아직 기입은 안 됩니다.

---

## 4. 구글시트 연결 (15분) — 가장 번거롭지만 한 번만 하면 끝

### 4-1. 서비스 계정 만들기

1. https://console.cloud.google.com 접속
2. 위쪽 프로젝트 선택 → **새 프로젝트** → 이름 `viral-insight` → 만들기
3. 검색창에 **Google Sheets API** → **사용** 버튼
4. 검색창에 **사용자 인증 정보** → **+ 사용자 인증 정보 만들기** → **서비스 계정**
   - 이름 `sheet-writer` → **만들고 계속하기** → **완료**
5. 만들어진 계정 클릭 → **키** 탭 → **키 추가** → **새 키 만들기** → **JSON** → 만들기
   → JSON 파일이 다운로드됩니다

### 4-2. 시트를 그 계정에 공유

1. 방금 만든 서비스 계정의 이메일을 복사합니다
   (`sheet-writer@viral-insight-xxxx.iam.gserviceaccount.com` 형태)
2. **콘텐츠RD 시트** 열기 → 오른쪽 위 **공유**
3. 그 이메일을 붙여넣고 **편집자** 로 지정 → **보내기**
   (알림 보내기 체크는 해제해도 됩니다)

### 4-3. Streamlit Secrets 에 추가

1. Streamlit Cloud → 내 앱 → 오른쪽 아래 **⋮** → **Settings** → **Secrets**
2. 기존 내용 **아래에** 다음을 추가합니다

```toml
[google_service_account]
type = "service_account"
project_id = "여기에 JSON의 project_id"
private_key_id = "여기에 JSON의 private_key_id"
private_key = "-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
client_email = "sheet-writer@....iam.gserviceaccount.com"
client_id = "여기에 JSON의 client_id"
token_uri = "https://oauth2.googleapis.com/token"
```

> ⚠️ **`private_key` 가 제일 실수가 잦습니다.** 다운로드한 JSON을 메모장으로 열면
> `private_key` 값 안에 `\n` 이 글자 그대로 들어 있습니다. **그대로 복사하세요.**
> 줄바꿈으로 바꾸면 안 됩니다. 큰따옴표도 그대로 둡니다.

3. **Save** → 앱이 자동 재시작됩니다

이제 담당자 화면 위쪽 **구글시트**가 `연결됨` 으로 바뀌어야 합니다.

---

## 5. 업체 토큰 바꾸기 (5분) — 저장소를 Public 으로 했다면 필수

`config.py` 에 적힌 기본 토큰은 저장소에 그대로 보입니다.
Secrets 에 아래를 추가하면 그것이 우선 적용됩니다.

```toml
[VENDORS]
"업크루" = "aX7k92Lm"
"굿띵투유" = "Qr4vN8pZ"
"루나앤코코" = "Ty6bW3sK"
"동후작가" = "Hn9cM5dR"
```

토큰은 아무 문자열이나 되지만 추측하기 어렵게 8자 이상으로 하세요.
업체를 추가할 때도 여기에 한 줄 넣으면 됩니다 (코드 수정 불필요).

---

## 6. 첫 운영 (10분)

1. 사이트 접속 → 비밀번호 입력
2. **업체 링크** 탭 → 위 칸에 사이트 주소 입력 → 업체별 링크가 만들어집니다
3. **판독·반영** 탭에서 먼저 테스트
   - 업체 `루나앤코코`, 기준일 `2026-08-23`, 폴더 기준 `이 날짜 업로드분만`
   - `루나앤코코\0823` 폴더 열고 `Ctrl+A` → 드래그 → **11장 판독**
   - 확정 11건이 나오면 정상입니다
   - **반영 버튼은 값을 확인한 뒤에** 누르세요
4. 잘 되면 업체 단톡방에 링크를 보냅니다

### 업체에 보낼 문구

> 안녕하세요! 인사이트 전달 방식을 바꾸려 합니다.
> 앞으로는 아래 링크에 스크린샷만 올려주시면 됩니다. 로그인 없습니다.
>
> 👉 (링크)
>
> 캡처 방식은 지금 그대로 하셔도 됩니다. 올리시면 바로 인식 결과가 보이니,
> 혹시 잘못 읽혔다고 나오면 그 화면만 다시 올려주세요. 감사합니다!

---

## 매일 운영

| | 하는 일 |
|---|---|
| 업체 | 링크 열고 스크린샷 드래그. 끝. |
| 담당자 | 아무것도 안 함. **확인 필요** 건만 사이트에서 행 지정 후 반영 |

**월요일**은 금·토·일 합산본 1개가 들어옵니다.
반영 전에 **"측정을 건너뛴 중간 날짜 열에 직전 측정값 복사"** 체크가 켜져 있는지
확인하세요 (기본 켜짐). 금·토 열에 직전 누적값이 복사되고 일요일 열에 새 값이 들어갑니다.
0을 넣으면 증분값이 왜곡되기 때문입니다.

---

## 문제가 생기면

| 증상 | 원인 / 조치 |
|---|---|
| `구글시트 미설정` | Secrets 의 `private_key` 에서 `\n` 이 줄바꿈으로 바뀐 경우가 대부분 |
| `추적 대상 게시물을 찾지 못했습니다` | 시트 업체명과 `config.py`/Secrets 의 업체명이 정확히 같아야 합니다 |
| `후보 행이 없습니다` | 폴더 기준(`추적중 전체` / `이 날짜 업로드분만`)을 바꿔보세요 |
| 업체가 "20초 넘게 안 열려요" | 무료 등급은 유휴 시 잠듭니다. 첫 접속만 느립니다 |
| 값이 이상하게 들어감 | 시트에서 그 칸만 직접 고치면 됩니다. 사이트는 해당 날짜 열 한 칸만 씁니다 |
| 판독이 자꾸 틀림 | 어떤 화면에서 틀리는지 알려주세요. `extract.py` 프롬프트를 조정합니다 |

## 안 하셔도 되는 것

- 로컬에 파이썬 설치 — 사이트에서 전부 됩니다
- 폴더 정리 — 카톡 이미지를 그대로 올리면 됩니다
- 업체에 캡처 형식 요구 — 형식이 달라도 읽습니다
